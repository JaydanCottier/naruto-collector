---
interface Props {
  compact?: boolean;
}
const { compact = false } = Astro.props;
---
{compact ? (
  <span class="text-[10px] text-brand-text-muted/60 font-medium">
    🔗 Affiliate link
  </span>
) : (
  <p class="text-[11px] text-brand-text-muted/60 font-medium mt-1.5">
    🔗 Affiliate link — we may earn a small commission at no cost to you
  </p>
)}
